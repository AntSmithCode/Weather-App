const geoHttps = `https://api.opencagedata.com/geocode/v1/json?q=LAT+LNG&key${auth}`;
const weatherHttps =
  "https://www.metaweather.com/api/location/search/?lattlong=(latt),(long)";
const zipWeatherElement = document.querySelector(".container");

// Fetch zip code data
async function getLocation() {}

// Use Fetched zip code data to Fetch zip code weather data

// Display Fetched zip code and weather data in Dom Elements

//<!-- Use all the weather data you receive from 7Timer to generate a 7 day forecast for the given zip code.
